import { NextRequest, NextResponse } from 'next/server';export function middleware(req:NextRequest){const host=req.headers.get('host')||'';const base=process.env.BASE_DOMAIN||'';let tenant='';if(base && host.endsWith(base)){const sub=host.replace('.'+base,'');if(sub && sub!==host) tenant=sub.toLowerCase();}const res=NextResponse.next();if(tenant){res.headers.set('x-tenant',tenant);}return res;}